These are certs from https://letsencrypt.org/docs/staging-environment/
and are not trusted by browsers. You can however change the `kcp-front-proxy`'s certificate to be issued
by staging Let's Encrypt.
